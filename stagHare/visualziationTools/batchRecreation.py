# so we should be able to do this from a live sim file and from a read in file. I will probably just amke a function that
# can preface it from something written somewhere.
# the problem is that threading low key destroys my old conceptions of loggers, so thats something I am trying to work around rn.


def recreate_batch(current_game_files):
    pass
    # aight what do I actually want to know?
    # maybe



















if __name__ == "__main__":
    current_batch_file = ""
    recreate_batch(current_batch_file)